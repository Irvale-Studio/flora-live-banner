name: flora_live_banner
description: Flora Live "Video identification" animated home banner for PlantSnap, built in Flutter.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.10

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  assets:
    - assets/flora_expert.png
